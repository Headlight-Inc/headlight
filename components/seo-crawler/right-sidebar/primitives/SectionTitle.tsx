import React from 'react'
export const SectionTitle = ({ children }: { children: React.ReactNode }) => (
    <div className="text-[10px] font-bold text-[#666] uppercase tracking-[0.14em] mb-1.5">{children}</div>
)
